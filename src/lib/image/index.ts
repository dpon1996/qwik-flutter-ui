/**
 * `Image` widget barrel. Re-exports the component and its props type.
 */

export { Image } from "./image";
export type {
  ImageErrorBuilder,
  ImagePlaceholderBuilder,
  ImageProps,
} from "./types";
