/**
 * `Center` prop types. See `docs/API_DESIGN.md` §10.
 */

import type { BaseProps } from "../_shared";

/**
 * Props for the `Center` widget — centers its child horizontally and
 * vertically. Flutter equivalent of `Center`.
 *
 * v1 intentionally keeps this surface minimal. `widthFactor` /
 * `heightFactor` are documented as v1.1 candidates in the API design doc.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface CenterProps extends BaseProps {}
