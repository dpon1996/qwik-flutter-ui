/**
 * v1.5 Theming — shared types and context (§57).
 */

export { ThemeContext } from "./context";

export type {
  ButtonTheme,
  ColorScheme,
  InputDecorationTheme,
  TextStyle,
  TextTheme,
  ThemeData,
} from "./types";
