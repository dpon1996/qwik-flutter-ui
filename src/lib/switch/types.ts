/**
 * `Switch` prop types. See `docs/API_DESIGN.md` §49.
 */

import type { CheckboxProps } from "../checkbox/types";

/** Same public API as `Checkbox` (Decision #81). */
export type SwitchProps = CheckboxProps;
