/**
 * SnackBar infrastructure constants (§80, OV14).
 */

/** Default auto-dismiss duration in ms (§80.1). */
export const DEFAULT_SNACKBAR_DURATION = 4000;

/** Host marker for SSR / future UI anchoring. */
export const SNACKBAR_HOST_DATA_ATTR = "data-qfu-snackbar-host";
